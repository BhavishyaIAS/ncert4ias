/**
 * Database types.
 *
 * Placeholder for M0. Once the Supabase schema lands in M1, this file will hold
 * the generated `Database` type (via `supabase gen types typescript`). Until
 * then it exports an empty shape so imports don't break.
 */
export type Database = Record<string, never>;
